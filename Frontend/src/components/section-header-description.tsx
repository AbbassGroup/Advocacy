const SectionHeaderDescription = ({ title }: { title: string }) => {
    return (
        <p className="text-muted-foreground text-lg leading-relaxed">
            {title}
        </p>
    )
}

export default SectionHeaderDescription